{
  "name": "student-logs-p3",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "server": "json-server db.json --routes routes.json",
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "concurrently \"npm run watch\" \"npm run server\""
  },
  "devDependencies": {
    "@types/jquery": "^3.5.32",
    "concurrently": "^9.1.2",
    "json-server": "0.17.0",
    "typescript": "^5.7.3"
  }
}
